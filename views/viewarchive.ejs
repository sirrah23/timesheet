<!DOCTYPE html>
<html>
    <head>
        <title><%= title  %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <h1><%= title %></h1>
        <% for(var i=0;i<data.length;i++){ %>
        <table>
        <tr><td colspan="2"  > <%= data[i].created %> </td></tr>
            <% for(var j=0;j<data[i].table.length;j++){ %>
                <tr> 
                    <td><%= data[i].table[j].task  %></td>
                    <td><%= data[i].table[j].time  %></td>         
                </tr>
            <% } %>
        </table>
        </br>
        <% } %>
    </body>
</html>

